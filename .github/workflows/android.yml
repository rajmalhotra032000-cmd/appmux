name: Android CI (Build APK)

on:
  workflow_dispatch:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: reactivecircus/android-sdk:34.0.0
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Container already includes Android SDK (platform-tools, build-tools, platforms)

      - name: Bootstrap Gradle (no wrapper yet)
        run: |
          curl -L -o gradle.zip https://services.gradle.org/distributions/gradle-8.7-bin.zip
          unzip -q gradle.zip -d $RUNNER_TEMP
          echo "GRADLE_HOME=$RUNNER_TEMP/gradle-8.7" >> $GITHUB_ENV
          echo "$RUNNER_TEMP/gradle-8.7/bin" >> $GITHUB_PATH

      - name: Generate Gradle Wrapper
        run: |
          gradle -v
          gradle wrapper --gradle-version 8.7

      - name: Build debug APK
        run: |
          chmod +x ./gradlew || true
          ./gradlew --no-daemon --stacktrace assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: Appmux-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk


